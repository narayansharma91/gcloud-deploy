steps:

- name: gcr.io/cloud-builders/docker
  args: [ build, -t, gcr.io/$PROJECT_ID/gcp-node-application, . ]
- name: gcr.io/cloud-builders/gcloud
  args: [ beta, compute, instance-groups, managed, rolling-action, start-update,  instance-group-2 , --version=template=instance-template-1, --region=asia-south1]

images:

- gcr.io/$PROJECT_ID/gcp-node-application
